Архитектура:

phone_book_OOP/
├── main.py              # Точка входа
├── controller.py        # Контроллер (логика приложения)
├── model.py             # Модель (работа с данными)
├── view.py              # Представление (интерфейс)
├── text.py              # Текстовые константы и сообщения
├── data.json            # Хранилище контактов (JSON)
└── README.md            # Документация

Возможности:
1. Открыть файл          # Загружает контакты из data.json
2. Сохранить файл        # Сохраняет изменения в data.json
3. Показать все контакты # Отображает все контакты в таблице
4. Создать контакт       # Добавление нового контакта
5. Найти контакт         # Поиск по имени/телефону/комментарию
6. Изменить контакт      # Редактирование существующего контакта
7. Удалить контакт       # Удаление контакта по индексу

📋 Принцип работы справочника
Основной поток работы:

    Загрузка данных — начинаем с пункта меню "1. Открыть файл", который загружает контакты из JSON-файла в память приложения.

    Работа с контактами — после загрузки доступны все операции:

        Пункт 2: Сохранить изменения в файл

        Пункт 3: Просмотреть все контакты в табличном формате

        Пункт 4: Создать новый контакт

        Пункт 5: Найти контакт по имени, номеру или комментарию

        Пункт 6: Изменить существующий контакт

        Пункт 7: Удалить контакт

Особенности операций:
🔍 Поиск контактов (пункт 5):

Поиск осуществляется по ключевым словам или числам. Для нахождения контакта достаточно начать вводить:

    Имя (полностью или частично, например: "Анд" для "Андрей")

    Номер телефона (полностью или фрагмент, например: "7999")

    Комментарий/тег (любое слово из описания, например: "python")

Примечание: Поиск не чувствителен к регистру и работает по всем полям одновременно.

Для операций изменения и удаления (пункты 6 и 7):

Поскольку идентификаторы (ID) контактов формируются динамически при каждом отображении, перед изменением или удалением необходимо открыть список всех контактов (пункт 3), чтобы увидеть актуальные номера.
Механизм удаления и изменения:

    Удаление — выполняется по ID контакта из таблицы

    Изменение — доступно по трём параметрам на выбор:

        Имя контакта

        Номер телефона

        Комментарий (тег)

После любых манипуляций с контактами (добавление, изменение, удаление), если нужно сохранить изменения на диск, необходимо выбрать пункт "2. Сохранить файл". Без этого действия все изменения останутся только в памяти приложения и будут потеряны после его закрытия.


